# Legacy Backend Reference

This directory contains the pre-LangChain `src/` tree.

Keep it as a reference while rebuilding the backend. Useful things to recover from here include:

- Previous FastAPI route behavior.
- Frontend API response shapes.
- Library, annotations, media, and session storage formats.
- Tool implementations that can be wrapped as LangChain tools later.
