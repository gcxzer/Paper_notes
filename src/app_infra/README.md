# app_infra

Small shared infrastructure helpers used across the backend.

## Files

- `__init__.py`: Package marker for shared infrastructure modules.
- `formatting.py`: Text normalization and lightweight formatting helpers.
- `paths.py`: Project, resource, notes, PDF, HTML, and annotation path constants.
- `storage.py`: Atomic JSON and text file write helpers.
