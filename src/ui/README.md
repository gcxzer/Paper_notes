# ui

HTTP backend routes and browser frontend assets.

## Subdirectories

- `backend/`: Python HTTP API handlers, local static server, settings APIs, memory APIs, skills APIs, model provider APIs, and agent chat endpoints.
- `frontend/`: HTML, JavaScript, and CSS for the library page, reader page, generated notes, PDF annotations, chat UI, and settings dialogs.
