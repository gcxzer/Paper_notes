# app_config

Local configuration and secret handling for model providers and runtime settings.

## Files

- `__init__.py`: Package marker for configuration helpers.
- `ai_settings.py`: Resolves and updates AI provider, model, OpenAI API key, and Codex OAuth settings.
- `secrets.py`: Defines `.paper-notes/` paths and reads/writes simple local environment files.
