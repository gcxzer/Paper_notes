[project]
name = "paper-notes"
version = "1.2.1"
description = "A local research workspace for reading PDFs, annotating papers, and building HTML notes with an AI assistant."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "beautifulsoup4>=4,<5",
  "fastapi>=0.124,<1",
  "matplotlib>=3.8,<4",
  "mcp>=1.24,<2",
  "numpy>=2,<3",
  "openai>=2.21.0,<3",
  "pandas>=2,<3",
  "pillow>=10,<13",
  "pymupdf>=1.24,<2",
  "requests>=2,<3",
  "uvicorn[standard]>=0.47,<1",
]

[dependency-groups]
dev = [
  "httpx>=0.28,<1",
  "pytest>=8.0",
  "ruff>=0.8",
]

[tool.uv]
default-groups = []
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "src/ui"]

[tool.ruff]
line-length = 120
target-version = "py312"
